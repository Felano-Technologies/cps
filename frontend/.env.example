# In local dev, requests to /api are proxied to the backend by Vite (see vite.config.ts),
# so VITE_API_URL can be left unset. Set it explicitly when pointing at a deployed backend.
VITE_API_URL=https://cps-production-6d97.up.railway.app/api
